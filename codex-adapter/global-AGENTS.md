<!-- geo-seo-codex-adapter -->
# GEO-SEO Codex compatibility

When a GEO skill or GEO custom agent is invoked, treat the original files installed under `~/.claude/skills/geo`, `~/.claude/skills/geo-*`, and `~/.claude/agents/geo-*.md` as the canonical instructions. Read and follow them directly and completely; do not rewrite, replace, summarize, or reinterpret the author's methodology. Use `~/.claude/skills/geo/.venv/Scripts/python.exe` for the original Python scripts on Windows without editing those scripts or prompts.
